name: 🚀 RDP555 TITAN MODE (1TB STORAGE + MAX SPEED)

on:
  workflow_dispatch:
    inputs:
      duration:
        description: '🕐 Session Duration'
        required: false
        default: '6h'
        type: choice
        options: ['1h', '3h', '6h']

jobs:
  RDP-Setup:
    runs-on: windows-latest
    timeout-minutes: 360
    
    steps:
      - name: 🎯 INITIALIZING TITAN SYSTEM
        run: Write-Host "🚀 STARTING TITAN MODE WITH 1TB STORAGE..." -ForegroundColor Cyan

      - name: 💾 CREATING 1TB VIRTUAL DISK
        run: |
          Write-Host "💽 Creating 1TB Virtual Hard Disk..." -ForegroundColor Yellow
          $vhdPath = "C:\1TB_Storage.vhd"
          # إنشاء ملف قرص افتراضي بمساحة 1024 جيجابايت
          @"
          create vdisk file="$vhdPath" maximum=1048576 type=expandable
          attach vdisk
          create partition primary
          format fs=ntfs quick label="TITAN_STORAGE"
          assign letter=E
          "@ | diskpart
          Write-Host "✅ 1TB Disk Created and Mounted as Drive (E:)" -ForegroundColor Green

      - name: 🏎️ HARDWARE & RAM BOOST (VIRTUAL RAM)
        run: |
          Write-Host "🧠 Boosting Virtual RAM to 16GB..." -ForegroundColor Yellow
          wmic computersystem where name="%computername%" set AutomaticManagedPagefile=False
          wmic pagefileset where name="C:\\pagefile.sys" set InitialSize=12288,MaximumSize=16384
          reg add "HKLM\System\CurrentControlSet\Control\PriorityControl" /v Win32PrioritySeparation /t REG_DWORD /d 38 /f
          Write-Host "✅ Hardware Tweaks Applied!" -ForegroundColor Green

      - name: 🧹 TOTAL DEBLOAT & PERFORMANCE
        run: |
          Write-Host "🧹 Stripping Windows for Maximum Speed..." -ForegroundColor Yellow
          $kill = @("wuauserv", "bits", "dosvc", "WSearch", "SysMain", "WerSvc", "Themes")
          foreach($s in $kill) { Stop-Service $s -Force -ErrorAction SilentlyContinue; Set-Service $s -StartupType Disabled }
          Get-AppxPackage -AllUsers | where {$_.Name -notlike "*Calculator*" -and $_.Name -notlike "*Paint*"} | Remove-AppxPackage -ErrorAction SilentlyContinue
          # تعطيل الحركات البصرية لتسريع الواجهة
          Set-ItemProperty -Path "HKCU:\Control Panel\Desktop" -Name "MenuShowDelay" -Value "0"
          Write-Host "✅ System is now Lightweight!" -ForegroundColor Green

      - name: 🛰️ ZERO-LATENCY NETWORK TWEAKS
        run: |
          netsh int tcp set global autotuninglevel=disabled
          netsh int tcp set global congestionprovider=ctcp
          netsh int tcp set global ecncapability=enabled
          netsh interface ipv4 set subinterface "Ethernet" mtu=1500 store=persistent
          Write-Host "✅ Network Optimized!" -ForegroundColor Green

      - name: ⚙️ CONFIGURING RDP (ULTRA SPEED)
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          bcdedit /set {current} testsigning off
          netsh advfirewall firewall add rule name="RDP" dir=in action=allow protocol=TCP localport=3389 profile=any
          Start-Service TermService -ErrorAction SilentlyContinue

      - name: 👤 CREATING USER
        run: |
          $Password = ConvertTo-SecureString "BASHAR12!" -AsPlainText -Force
          New-LocalUser -Name "rdp555" -Password $Password -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member "rdp555"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "rdp555"

      - name: 🌐 TAILSCALE CONNECTION
        env:
          TAILSCALE_AUTH_KEY: ${{ secrets.TAILSCALE_AUTH_KEY }}
        run: |
          $msiPath = "$env:TEMP\tailscale.msi"
          Invoke-WebRequest -Uri "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi" -OutFile $msiPath
          Start-Process msiexec.exe -ArgumentList "/i", "`"$msiPath`"", "/quiet", "/norestart" -Wait
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=$env:TAILSCALE_AUTH_KEY --hostname=rdp555-titan
          $ip = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
          Write-Host "🌐 IP: $ip | USER: rdp555 | PASS: BASHAR12!" -ForegroundColor White

      - name: ♾️ KEEP ALIVE
        run: for ($i = 1; $i -le 350; $i++) { Start-Sleep -Seconds 60 }
