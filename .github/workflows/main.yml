name: 🚀 RDP555 PRO-TWEAKED (ULTRA FAST + CHRIS TITUS TWEAKS)

on:
  workflow_dispatch:
    inputs:
      duration:
        description: '🕐 Session Duration'
        required: false
        default: '6h'
        type: choice
        options: ['1h', '3h', '6h']

jobs:
  RDP-Setup:
    runs-on: windows-latest
    timeout-minutes: 360
    
    steps:
      - name: 🎯 INITIALIZING PRO SYSTEM
        run: Write-Host "🚀 STARTING PRO-TWEAKED MODE..." -ForegroundColor Cyan

      - name: 🛠️ APPLYING CHRIS TITUS & SYSTEM TWEAKS
        run: |
          Write-Host "⚡ Running Chris Titus Tech Tweaks & System Optimization..." -ForegroundColor Yellow
          
          # 1. تفعيل وضع الأداء الأقصى وإلغاء قيود الطاقة
          powercfg /duplicatescheme e9a42b02-d5df-448d-aa00-03f14749eb61
          powercfg /setactive e9a42b02-d5df-448d-aa00-03f14749eb61
          
          # 2. تطبيق تويكات النظام العميقة (Registry Tweaks) لسرعة البرق
          # تقليل وقت استجابة الماوس والكيبورد والقوائم
          reg add "HKCU\Control Panel\Desktop" /v MenuShowDelay /t REG_SZ /d 0 /f
          reg add "HKLM\SYSTEM\CurrentControlSet\Control\PriorityControl" /v Win32PrioritySeparation /t REG_DWORD /d 38 /f
          
          # 3. تعطيل الـ Telemetry والخدمات المزعجة نهائياً
          reg add "HKLM\SOFTWARE\Policies\Microsoft\Windows\DataCollection" /v AllowTelemetry /t REG_DWORD /d 0 /f
          
          # 4. محاكاة عمل أدوات مثل DumpApps و Optimizer
          $services = @("SysMain", "DiagTrack", "WSearch", "WerSvc", "wuauserv", "bits")
          foreach($s in $services) { Stop-Service $s -Force -ErrorAction SilentlyContinue; Set-Service $s -StartupType Disabled }
          
          Write-Host "✅ System Tweaks Applied!" -ForegroundColor Green

      - name: 🧠 EXTREME RAM BOOST (24GB VIRTUAL RAM)
        run: |
          Write-Host "🧠 Boosting Virtual RAM for Heavy Dev Work..." -ForegroundColor Yellow
          wmic computersystem where name="%computername%" set AutomaticManagedPagefile=False
          wmic pagefileset where name="C:\\pagefile.sys" set InitialSize=20480,MaximumSize=24576
          Write-Host "✅ 24GB Virtual RAM Ready!" -ForegroundColor Green

      - name: 💾 CREATING GIANT STORAGE (34TB, 12TB, 2TB)
        run: |
          @"
          create vdisk file="C:\Disk_34TB.vhd" maximum=35651584 type=expandable
          attach vdisk
          create partition primary
          format fs=ntfs quick label="ULTRA_STORAGE_34TB"
          assign letter=E
          create vdisk file="C:\Disk_12TB.vhd" maximum=12582912 type=expandable
          attach vdisk
          create partition primary
          format fs=ntfs quick label="MID_STORAGE_12TB"
          assign letter=F
          create vdisk file="C:\Disk_2TB.vhd" maximum=2097152 type=expandable
          attach vdisk
          create partition primary
          format fs=ntfs quick label="STORAGE_2TB"
          assign letter=G
          "@ | diskpart

      - name: ⚙️ CONFIGURING RDP (ULTRA LOW LATENCY)
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          bcdedit /set {current} testsigning off
          netsh advfirewall firewall add rule name="RDP" dir=in action=allow protocol=TCP localport=3389 profile=any
          Start-Service TermService -ErrorAction SilentlyContinue

      - name: 👤 CREATING USER
        run: |
          $Password = ConvertTo-SecureString "BASHAR12!" -AsPlainText -Force
          New-LocalUser -Name "rdp555" -Password $Password -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member "rdp555"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "rdp555"

      - name: 🌐 TAILSCALE CONNECTION
        env:
          TAILSCALE_AUTH_KEY: ${{ secrets.TAILSCALE_AUTH_KEY }}
        run: |
          $msiPath = "$env:TEMP\tailscale.msi"
          Invoke-WebRequest -Uri "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi" -OutFile $msiPath
          Start-Process msiexec.exe -ArgumentList "/i", "`"$msiPath`"", "/quiet", "/norestart" -Wait
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=$env:TAILSCALE_AUTH_KEY --hostname=rdp555-pro
          $ip = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
          Write-Host "🌐 IP: $ip | USER: rdp555 | PASS: BASHAR12!" -ForegroundColor White

      - name: ♾️ KEEP ALIVE
        run: for ($i = 1; $i -le 350; $i++) { Start-Sleep -Seconds 60 }
